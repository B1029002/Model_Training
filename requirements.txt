# Core training dependencies
transformers>=4.40.0
datasets>=2.16.0
accelerate>=0.27.0
peft>=0.9.0

# DeepSpeed for multi-GPU training
deepspeed>=0.13.0

# Flash Attention 2 (install separately based on CUDA version)
# pip install flash-attn --no-build-isolation

# Tokenizers
tokenizers>=0.15.0
sentencepiece>=0.1.99

# Logging and monitoring
wandb>=0.16.0
tensorboard>=2.15.0

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
scipy>=1.11.0

# Evaluation
evaluate>=0.4.0
scikit-learn>=1.3.0
